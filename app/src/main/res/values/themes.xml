<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MobileDDAssistant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Светлая тема -->
        <item name="colorBackground">@color/background_color</item>
        <item name="colorContent">@color/content_color</item>
        <item name="colorText">@color/text_color</item>
        <item name="colorHint">#888888</item>
        <item name="colorBars">@color/bars_color</item>
        <item name="colorIcon">@color/icon_color</item>
        <item name="colorButton">@color/button_color</item>
        <item name="colorButtonText">@android:color/white</item>
        <item name="colorCard">@color/cards</item>

        <item name="android:windowBackground">?attr/colorBackground</item>
    </style>

    <style name="Theme.MobileDDAssistant" parent="Base.Theme.MobileDDAssistant" />
</resources>